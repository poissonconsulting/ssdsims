## Context

`ssd-define-scenario` landed the declarative `ssdsims_scenario` object (`TARGETS-DESIGN.md` §1): seed, knobs, dataset names, fit/hc argument grids, `partition_by`, and the recorded `ci = FALSE` ignore. Nothing consumes it yet. The roadmap (§12) makes `task-list-loop-baseline` the immediate next step on the `define → baseline` edge: turn the scenario into the **flat per-task data shape** that all later steps build on, plus a no-frills runner.

The §2 "What goes into the hash" discussion already specifies the per-step identity axes — data: `(dataset, sim, replace)`; fit: data-identity + fit-grid row; hc: fit-identity + hc-grid row (modulo the §1.2 collapse). This step materializes exactly those tables as plain tibbles, with **no** RNG/primer columns and **no** targets/shard machinery. The point is to freeze the data shape early so subsequent steps (`task-primer`, `state-primitives`, `task-tables`) each change one thing against a working baseline rather than introducing the shape and the shards and the RNG all at once.

## Goals / Non-Goals

**Goals:**
- Three derivation functions producing `data`, `fit`, and `hc` task tables (tibbles) from a scenario, one column per cross-join axis.
- Honour the §1.2 `ci = FALSE` collapse at hc-table expansion using the flag the scenario already records.
- A baseline runner: three `purrr::pmap()` loops, data → fit → hc, threading outputs forward, returning collected results.
- Establish the column contract that `task-tables` later decorates with `(seed, primer)` and groups into shards.

**Non-Goals:**
- No RNG, no `(seed, primer)`/`stream` columns (those land in `task-primer` / `state-primitives`).
- No `targets` dependency, no `tar_map`, no shards, no `partition_by` grouping (those land in `task-tables`).
- No Hive partitioning, no Parquet I/O (later steps).
- No sub-truncation behaviour for `nrow` (the §5 property; here `nrow` is merely carried as a column).
- No change to `ssd_run_scenario()` or the existing public pipeline.

## Decisions

- **Decision: four separate tables, not one wide join.** The pipeline fans out in four steps with different fan-outs and (later) different shard axes (§5 `partition_by` defaults differ per step). Keeping `sample`/`data`/`fit`/`hc` as distinct tibbles, each referencing its parent by an id foreign key, mirrors the §6 step structure and lets `task-tables` group each independently. *Alternative*: a single fully-joined table — rejected; it conflates the per-step shard boundaries and bloats rows.
- **Decision: sampling is its own task type; `nrow` is an axis of truncation, not of the draw.** Reviewer-driven (PR #80). §5 proves every `nrow` value is a sub-truncation of one `n_max`-row draw. Rather than carry `nrow` as a compound column on a conflated `data` step, the draw becomes a distinct `sample` task keyed `(dataset, sim, replace)` carrying `n_max`, and a separate RNG-free `data` step truncates it to each `nrow` (`head(sample, nrow)`). So `nrow` is a clean scalar cross-join axis from the `data` step down, the expensive draw is shared *structurally* (one `sample` task per `(dataset, sim, replace)`), and the §5 efficiency holds without a carried-column trick. *Alternative A*: carry `nrow` as a list column on a single `data` step — rejected (the compound column the review flagged). *Alternative B*: carry a scalar `n_max` on a single `data` step and truncate at consumption — rejected; it leaves `nrow` with no clean home as an axis and hides the truncation. *Alternative C*: make `nrow` a cross-join axis of the *draw* — rejected; it re-draws redundantly and contradicts §5.
- **Decision: explicit path-style `<step>_id` keys and parent foreign keys.** Reviewer-driven (PR #80). §5 anticipates `data_id` / `fit_id` columns as "sugar for the path". Each table carries a path-style primary key built from its cross-join axes (the §6 Hive partition path, e.g. `dataset=boron/sim=1/replace=FALSE`) plus its parent's id as a foreign key, so a child references its parent by one joinable column and the runner threads via the FK. The id is deterministic and stable under scenario growth (adding a dataset does not renumber existing rows), matching the §8 cache-by-existence story. *Alternative*: sequential integer ids — rejected; they renumber on growth. *Alternative*: implicit composite key (carried identity columns only) — rejected; the reviewer wants an explicit single-column foreign key, and §5 calls for the path id.
- **Decision: `replace` is a first-class scenario knob.** Reviewer-driven (PR #80). `replace` is a sampling axis on a par with `nrow`, already a knob on `ssd_sim_data()` / `ssd_run_scenario()`. `ssd_define_scenario()` gains a validated `replace` argument (logical, length ≤ 2, default `FALSE`) and the `sample` derivation reads `scenario$replace`. *Alternative*: default it to `FALSE` inside the derivation — rejected; it hid a real axis and the scenario is the single source of truth for the grids.
- **Decision: the scenario retains the data it was built from.** Reviewer-driven (PR #80). The scenario keeps the validated `ssd_data()` collection in `$data` (and `$datasets` keeps the names), so a local run reads the data straight off the scenario and `ssd_run_scenario_baseline(scenario)` needs no `data` argument. The names remain what the targets/cluster path hashes and resolves through the registry (§1.1), so that path still need not carry the data frames. *Alternative*: keep the scenario a names-only manifest and pass `data` to the runner — rejected; redundant for local use and the scenario is meant to be self-sufficient. *Alternative*: a registry now — out of scope (future step).
- **Decision: reuse the scenario's recorded `ci = FALSE` collapse.** The collapse is decided at construction time and recorded on the scenario (`ssd-define-scenario` §1.2). The hc derivation honours that flag rather than re-deriving the rule, keeping a single source of truth. *Alternative*: re-implement the collapse here — rejected; duplicates logic and risks drift.
- **Decision: `min_pmix` carried by name.** Per §1.1/§2, function-valued knobs are referenced by name so later hashing is stable. The fit table stores the name; value lookup is a later step's concern. *Alternative*: embed the function — rejected; defeats the name-only indirection.
- **Decision: `tidyr::expand_grid()` / `dplyr::cross_join()` for the cross-joins; `purrr::pmap()` for the runner.** Both are already in `Imports` and match the §12 bullet ("`purrr::pmap()` loops"). *Alternative*: a `targets` plan — rejected; explicitly out of scope and a later step.
- **Decision: task tables are a classed tibble (`ssdsims_tasks`) with a `print()` method; a compound `ssd_scenario_tasks()` bundles all four into an `ssdsims_task_set`.** A raw tibble prints as an undifferentiated grid; a thin S3 subclass that records the step lets `print.ssdsims_tasks()` lead with the step name, the cross-join axes, and the task count before a compact row preview — matching `print.ssdsims_scenario()`. Subclassing a tibble keeps every dplyr/tidyr verb working (they operate on the underlying tibble; the class is reattached or harmlessly dropped). The compound `ssd_scenario_tasks()` is the canonical expansion entry point (§1/§2) and returns an `ssdsims_task_set` exposing the four independently-classed tables (so `task-tables` can still group each on its own shard boundary). *Alternative*: return bare tibbles — rejected; the step/axis metadata and the nice printing have nowhere else to live.
- **Decision: the runner is "stupid" — it consumes the derived tables, it does not expand.** Reviewer-driven (PR #80). The runner calls `ssd_scenario_tasks()` and loops over each table with `purrr::pmap()`, resolving each task's parent result by the parent's id foreign key (no in-runner cross-join, no key reconstruction). It reuses the per-step operations (slice-sample / `head()` / `ssd_fit_dists` / `ssd_hc`) without RNG seeding, in plain order. The baseline does not need reproducibility yet; `state-primitives` introduces per-task seeding on top of this shape. *Alternative*: re-cross-join the grids inside the runner — rejected; it duplicates the derivation and is exactly what the review flagged.

## Risks / Trade-offs

- **Shape churn in later steps** → Mitigated by deriving the axes directly from §2's hash contract, so `task-tables` adds columns (`seed`, `primer`) rather than reshaping. Snapshot tests pin the column contract so later changes are visible diffs.
- **`ci = FALSE` collapse divergence from the scenario** → Mitigated by consuming the scenario's recorded flag rather than re-deriving; a test asserts the hc table row count matches the §1.2 example grid.
- **Baseline runner is not reproducible (no seeding)** → Accepted and intentional for this step; documented as a non-goal. Reproducibility arrives with `state-primitives`. Tests for the runner assert structure/threading, not draw values.
- **`sample` vs `data` naming could confuse readers** → Mitigated with roxygen and an inline comment: `sample` is the `n_max`-row draw keyed `(dataset, sim, replace)`; `data` is its `head(., nrow)` truncation. Pulling the truncation split forward also partly implements the `nrow-sub-truncation` roadmap item — accepted, because freezing a 3-step shape that step would tear out is the churn this baseline exists to avoid.
- **Four-type shape revises the north star** → `TARGETS-DESIGN.md` §5 is updated to describe `sample`/`data`/`fit`/`hc` and the path-id foreign keys; the partition_by defaults and target graph are reconciled in the later `task-tables` step.

## Migration Plan

Additive. New `R/task-lists.R` and tests; new exports + `man/` pages. No existing function changes, no data migration, no new dependency. The existing `ssd_run_scenario()` path is untouched and coexists. Fully reversible by removing the new file and its `NAMESPACE` entries.

## Open Questions

Resolved during implementation (reviewer-driven on PR #80):
- **Exported names**: `ssd_scenario_sample_tasks()` / `_data_tasks()` / `_fit_tasks()` / `_hc_tasks()`, the compound `ssd_scenario_tasks()`, and `ssd_run_scenario_baseline()`.
- **Runner return shape**: a named list of the four result tibbles (`sample`/`data`/`fit`/`hc`), each the task table plus a result list column.
- **Dependency encoding**: a path-style `<step>_id` primary key per table plus the parent's id as an explicit foreign key (`sample_id`/`data_id`/`fit_id`), carried alongside the verbatim identity columns.
