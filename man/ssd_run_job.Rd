% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-job.R
\name{ssd_run_job}
\alias{ssd_run_job}
\title{Run a Job (Subset of Tasks)}
\usage{
ssd_run_job(tasks, scenario)
}
\arguments{
\item{tasks}{A subset of the tibble returned by
\code{\link[=ssd_scenario_tasks]{ssd_scenario_tasks()}}. May be one or many rows.}

\item{scenario}{The \code{ssdsims_scenario} object the \code{tasks} were
derived from.}
}
\value{
A tibble matching the shape produced by
\code{\link[=ssd_run_scenario2]{ssd_run_scenario2()}} for the supplied rows.
}
\description{
Runs one or more tasks (rows from \code{\link[=ssd_scenario_tasks]{ssd_scenario_tasks()}}) using the
shared parameters in \code{scenario}, and returns a tibble of results with
\code{data}, \code{fits} and \code{hc} list-columns. Designed to be called from
inside a \code{targets} target so the result can be written to one Parquet
file per job.
}
\examples{
scenario <- ssd_sim_data2(ssddata::ccme_boron, nsim = 2)
tasks <- ssd_scenario_tasks(scenario)
ssd_run_job(tasks[1, , drop = FALSE], scenario)
}
