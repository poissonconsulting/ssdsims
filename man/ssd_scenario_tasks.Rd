% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-job.R
\name{ssd_scenario_tasks}
\alias{ssd_scenario_tasks}
\title{Expand a Scenario into its Task Grid}
\usage{
ssd_scenario_tasks(scenario)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} object built by
\code{\link[=ssd_sim_data2]{ssd_sim_data2()}}.}
}
\value{
A tibble with one row per task.
}
\description{
Returns the fully-expanded tibble of tasks that
\code{\link[=ssd_run_scenario2]{ssd_run_scenario2()}} iterates over. Each row is a single
(sim, stream, ...) task with its own L'Ecuyer-CMRG substream states.
Exposed for \code{targets}/\code{tarchetypes} pipelines that partition tasks
into jobs (one Parquet per job).
}
\examples{
tasks <- ssd_scenario_tasks(ssd_sim_data2(ssddata::ccme_boron, nsim = 2))
nrow(tasks)
}
