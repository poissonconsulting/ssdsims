% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{scenario_min_pmix}
\alias{scenario_min_pmix}
\title{Isolate a Materialised \code{min_pmix} Function from a Scenario by Name}
\usage{
scenario_min_pmix(scenario, name)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{name}{A scalar string naming one of the scenario's \code{min_pmix} entries.}
}
\value{
The single-argument \code{min_pmix} function stored under \code{name}.
}
\description{
Returns the single-argument \code{min_pmix} function materialised on \code{scenario}
under \code{name}. \code{\link[=ssd_define_scenario]{ssd_define_scenario()}} resolves each \code{min_pmix} reference to a
function once, at construction (so a cluster worker needs no shared
interactive environment), and stores it keyed by name; this accessor isolates
it. Aborts with an informative error when \code{name} is not one of the scenario's
\code{min_pmix} names.
}
\details{
Names - not function values - drive task hashing (\code{TARGETS-DESIGN.md} section
1.1): the fit-task path carries the \code{min_pmix} \emph{name}, and this accessor
resolves it back to the function at run time, so the function value never
enters a task identity (no byte-stability concern from byte-compilation or
captured environments).
}
\examples{
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 1L, seed = 42L)
scenario_min_pmix(scenario, "ssd_min_pmix")
}
\seealso{
\code{\link[=scenario_dataset]{scenario_dataset()}} for the dataset counterpart.
}
