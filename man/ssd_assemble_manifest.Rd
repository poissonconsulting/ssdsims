% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{ssd_assemble_manifest}
\alias{ssd_assemble_manifest}
\title{Assemble \code{completed_shards} from the Shards on Disk}
\usage{
ssd_assemble_manifest(dir)
}
\arguments{
\item{dir}{The results directory to write \code{manifest.json} into (created if it
does not exist).}
}
\value{
The merged manifest (head plus \code{completed_shards}) as an R list,
invisibly.
}
\description{
Builds the manifest's \code{completed_shards} map - shard partition path to
\code{{ sha256 }} - from the shard Parquets present under \code{dir}, so the manifest
reflects the set of shards whose Parquet exists (\code{TARGETS-DESIGN.md} section
8.5). A shard's per-shard sidecar (\code{\link[=ssd_record_shard]{ssd_record_shard()}}) is preferred where
present (carrying the trusted-as-produced sha256); otherwise the shard's
Parquet is hashed directly. Assembly therefore needs \strong{no} hook into the
pipeline runner - the shards are the truth. A shard whose Parquet is absent
is absent from \code{completed_shards}.
}
\details{
The assembled tail is merged into the manifest head at \verb{<dir>/manifest.json}
(written by \code{\link[=ssd_write_manifest]{ssd_write_manifest()}}) and written back, so \code{\link[=ssd_read_manifest]{ssd_read_manifest()}}
returns head and tail together.
}
\seealso{
\code{\link[=ssd_record_shard]{ssd_record_shard()}}, \code{\link[=ssd_write_manifest]{ssd_write_manifest()}}.
}
\keyword{internal}
