% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{ssd_open_uploaded}
\alias{ssd_open_uploaded}
\title{Open Uploaded Results for Querying, In Place}
\usage{
ssd_open_uploaded(upload, step)
}
\arguments{
\item{upload}{An upload destination from \code{\link[=ssd_upload_azure]{ssd_upload_azure()}} or
\code{\link[=ssd_upload_dryrun]{ssd_upload_dryrun()}}.}

\item{step}{One of \code{"sample"}, \code{"fit"}, \code{"hc"} (the step layer to read), or
\code{"summary"} (the uploaded combined summary).}
}
\value{
A lazy, \code{dplyr}-composable table over the uploaded results.
}
\description{
A generic, dispatched on the upload object's class, that opens the
\strong{uploaded} results so a user can read them back and confirm they landed
right after an upload (\code{TARGETS-DESIGN.md} section 6.1). For an Azure
destination it returns a \strong{lazy} \code{duckplyr}/DuckDB table over the Hive glob
\verb{<container>[/<prefix>]/<step>/**/part.parquet} (honouring the destination's
optional \code{prefix} subdirectory), read \strong{in place} via DuckDB's \code{azure}
extension (predicate pushdown straight against blob storage - \strong{no
download}), composable with \code{dplyr} verbs so a one-line
\code{ssd_open_uploaded(upload, step) |> dplyr::count()} is the immediate
post-upload smoke test. It resolves the \strong{same} front-end \verb{SSDSIMS_AZURE_*}
credentials as the write path and remaps them into a DuckDB \code{azure} secret
for the backend read, aborting (naming the missing requirement) when the
\code{azure} extension or a required credential is absent. For a dry-run
destination it aborts: a dry run uploads nothing, so the local shards should
be read directly.
}
\examples{
\dontrun{
upload <- ssd_upload_azure("https://acct.blob.core.windows.net", "results")
ssd_open_uploaded(upload, "hc") |> dplyr::count()
}
}
\seealso{
\code{\link[=ssd_upload_shard]{ssd_upload_shard()}}, \code{\link[=ssd_test_upload]{ssd_test_upload()}}.
}
