% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen.R
\name{ssd_gen}
\alias{ssd_gen}
\title{Materialise Generator Datasets for a Simulation Scenario}
\usage{
ssd_gen(..., .n, .seed)
}
\arguments{
\item{...}{One or more generator inputs (a function, a function-name
string, a \code{fitdists} object, or a \code{tmbfit} object), optionally named.}

\item{.n}{A scalar whole number: the number of rows each generator
materialises. Required. (This is the generated population size; the
scenario's \code{nrow} controls the resample/truncation sizes.)}

\item{.seed}{A scalar whole number: the base seed for generation.
Required, and independent of the scenario's \code{seed}.}
}
\value{
An \code{ssdsims_gen} object: a named list of validated \code{Conc} tibbles
of \code{.n} rows, for use within (or splicing into) \code{\link[=ssd_scenario_data]{ssd_scenario_data()}}.
}
\description{
Accepts generator-style inputs and materialises each, once, to a
validated tibble with a numeric \code{Conc} column of \code{.n} rows. The four
generator kinds are:
}
\details{
\itemize{
\item a \strong{function} taking the number of rows as its first argument (e.g.
\code{ssdtools::ssd_rlnorm});
\item a \strong{function-name string} (e.g. \code{"ssd_rlnorm"}), resolved as a bare name
in the caller's environment and then the \code{ssdtools} namespace (never via
parsing code); the string is also the dataset name;
\item a \strong{\code{tmbfit}} object (one distribution of a \code{\link[ssdtools:ssd_fit_dists]{ssdtools::ssd_fit_dists()}}
fit): drawn from the matching \verb{ssd_r<dist>} function with the fit's
estimates;
\item a \strong{\code{fitdists}} object: the top-weighted distribution is selected and
drawn as for a \code{tmbfit}.
}

A data frame is \strong{not} a generator - pass it directly to
\code{\link[=ssd_scenario_data]{ssd_scenario_data()}}.

The result is an \code{ssdsims_gen} collection designed to compose with
\code{\link[=ssd_scenario_data]{ssd_scenario_data()}} in two equivalent ways: as an unnamed argument
(flattened in) or spliced with \verb{!!!}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ssd_scenario_data(boron = ccme_boron, ssd_gen(synth = ssd_rlnorm, .n = 30, .seed = 1L))
ssd_scenario_data(boron = ccme_boron, !!!ssd_gen(synth = ssd_rlnorm, .n = 30, .seed = 1L))
}\if{html}{\out{</div>}}
}
\section{Reproducibility: \code{.seed} and the name-keyed streams}{
\code{.n} and \code{.seed} are \strong{required}: irreproducible or unsized generation is
impossible by construction. They are dot-prefixed formals, so they are never
absorbed into \code{...} and a generator named \verb{seed =} or \verb{n =} is never
partial-matched onto them. Each generator draws under a scoped dqrng state
seeded by \code{.seed} with its dataset \strong{name} as the dqrng stream
(\code{\link[=task_primer]{task_primer()}} over \code{list(dataset = name)}), so a single \code{.seed} fans out
across all generators in the call on independent, name-keyed streams. One
\code{.n} applies per call; for differing sizes, splice several \code{ssd_gen()}
calls together. The global \code{.Random.seed} is unchanged on return.

Names are taken from the argument names where supplied, otherwise the
function-name string itself, otherwise derived from the argument expression
by symbol capture (e.g. \code{ssdtools::ssd_rlnorm} becomes \code{"ssd_rlnorm"}); an
input with no derivable name (e.g. an anonymous function literal) must be
given an explicit name. Names must be unique across the call.
}

\section{dqrng-backed generation}{
Generation draws under the dqrng \code{pcg64} backend (\code{\link[=local_dqrng_backend]{local_dqrng_backend()}}),
with each generator seeded through \code{\link[=local_dqrng_state]{local_dqrng_state()}}, which brackets the
draw with the per-task dqrng-integrity witness: it aborts if a generator
escapes the backend (e.g. switches \code{RNGkind()} and draws from base R), since
such draws are not reproducible under \code{.seed}. A generator that consumes no
randomness passes.
}

\examples{
ssd_gen(synth = ssdtools::ssd_rlnorm, .n = 30, .seed = 42)

# one .seed fans out across generators on independent name-keyed streams
ssd_gen(a = ssdtools::ssd_rlnorm, b = ssdtools::ssd_rlnorm, .n = 30, .seed = 42)

# composes with data frames in ssd_scenario_data()
ssd_scenario_data(
  boron = ssddata::ccme_boron,
  ssd_gen(synth = ssdtools::ssd_rlnorm, .n = 30, .seed = 42)
)
}
\seealso{
\code{\link[=ssd_scenario_data]{ssd_scenario_data()}}, \code{\link[=ssd_define_scenario]{ssd_define_scenario()}},
\code{\link[=local_dqrng_backend]{local_dqrng_backend()}}, \code{\link[=task_primer]{task_primer()}}.
}
