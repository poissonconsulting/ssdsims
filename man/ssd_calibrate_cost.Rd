% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-estimate.R
\name{ssd_calibrate_cost}
\alias{ssd_calibrate_cost}
\title{Calibrate the Per-task Cost Model on the Current Machine}
\usage{
ssd_calibrate_cost(
  nboot = c(20L, 50L, 100L, 200L),
  nrow = c(5L, 10L, 20L, 50L),
  data = NULL,
  seed = 42L
)
}
\arguments{
\item{nboot}{An integer vector of tiny bootstrap sizes to sweep (the slope and
floor are fit over these).}

\item{nrow}{An integer vector of at least two sample sizes to fit one model
per (the \code{nrow_factor} is derived from these).}

\item{data}{A reference data frame with a numeric \code{Conc} column to resample
(default \code{ssddata::ccme_boron}).}

\item{seed}{A whole number seeding the resampling so the sweep is reproducible.}
}
\value{
An \code{ssdsims_cost_calibration} object with per-\code{ci_method}
\code{base}/\code{slope}/\code{n0} coefficients, a bounded \code{nrow_factor}, a \code{fixed_addend},
and provenance (cpu, R version, ssdtools version, date, sweep grid).
}
\description{
Runs a small, fixed benchmark sweep on the \emph{current} architecture - tiny
\code{nboot} values, all \code{ssdtools::ssd_ci_methods()}, and a couple of \code{nrow}
values - times each \code{ssdtools::ssd_hc()} call, fits the per-\code{ci_method} cost
model \code{time = (base + slope * max(nboot, n0)) * nrow_factor(nrow)}, and returns
a versioned \code{ssdsims_cost_calibration} object carrying the fitted coefficients
and provenance. Producing an architecture-specific estimator is nothing more
than this single call plus passing the result to \code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}}.
}
\details{
The sweep is self-contained and dependency-light: it draws data by resampling
a reference dataset (\code{ssddata::ccme_boron} by default) and times with base
\code{\link[=system.time]{system.time()}}. It takes minutes - not the hours a real scenario costs -
because the \code{nboot} values are tiny (the slope and floor are estimable from
small bootstraps). The one-time research that discovered the model's \emph{form}
(which axes are free, the \code{max(nboot, n0)} shape, the non-monotonic \code{nrow}
factor) is preserved under the change's \verb{exploration/} directory; it is not
rerun here.
}
\examples{
\dontrun{
calibration <- ssd_calibrate_cost()
calibration
}
}
\seealso{
\code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}} for the shipped default and
\code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}} to apply the result.
}
