% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-lists.R
\name{ssd_run_scenario_baseline}
\alias{ssd_run_scenario_baseline}
\title{Run a Scenario with the Baseline Loop Runner}
\usage{
ssd_run_scenario_baseline(scenario)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}
}
\value{
A named list with \code{sample}, \code{fit}, and \code{hc} elements: each the
corresponding task table augmented with a list column of per-task results
(\code{sample} draws, \code{fits} objects, and \code{hc} tibbles).
}
\description{
Executes the three task tables in dependency order - \code{sample}, \code{fit}, then
\code{hc} - by looping over each table with \code{purrr::pmap()} and looking up each
task's parent result by the parent's \verb{<step>_id} foreign key. The \code{fit} step
truncates its parent sample inline (\code{head(sample, nrow)}) before fitting. The
runner does no task expansion of its own (it consumes \code{\link[=ssd_scenario_tasks]{ssd_scenario_tasks()}});
it just threads outputs forward and returns the collected per-step results.
}
\details{
This is the no-frills baseline: it runs in-process, with \strong{no} \code{targets}
dependency, \strong{no} shard grouping or \code{partition_by}, and \strong{no} Parquet I/O.

It \strong{is} reproducible without an external seed. The runner opens one
\code{\link[=local_dqrng_backend]{local_dqrng_backend()}} scope and seeds each \code{sample}/\code{fit}/\code{hc} task exactly
once through its \verb{*_data_task_primer()} wrapper, with \code{seed = scenario$seed}
and a per-task primer derived from the task's canonical identity
(\code{\link[=task_primer]{task_primer()}} over the \code{task_axes(step)} columns). Because each task's
\verb{(seed, primer)} pair fully determines its RNG starting point, two runs of a
scenario with a fixed \code{seed} yield identical results, and a task's result is
independent of the order in which tasks run. These same
\verb{*_data_task_primer()} wrappers are the per-task entry point a future
\code{targets} shard body and the replay helper (\code{TARGETS-DESIGN.md} §7) reuse.

The scenario retains the data frames it was built from, so the runner reads
them directly - no separate \code{data} argument. \code{min_pmix} names are resolved to
their materialised functions off the scenario via \code{\link[=scenario_min_pmix]{scenario_min_pmix()}}
(resolved once, at construction), not by a runtime \code{ssdtools}/global-env
search.
}
\examples{
scenario <- ssd_define_scenario(
  ssddata::ccme_boron,
  nsim = 1L,
  nrow = 6L,
  seed = 42L,
  dists = "lnorm"
)
out <- ssd_run_scenario_baseline(scenario)
out$hc
}
