% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-lists.R
\name{ssd_run_scenario_baseline}
\alias{ssd_run_scenario_baseline}
\title{Run a Scenario with the Baseline Loop Runner}
\usage{
ssd_run_scenario_baseline(scenario, data)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{data}{An \code{\link[=ssd_data]{ssd_data()}} collection, a named list of data frames, or - for
a single-dataset scenario - one data frame, supplying the data referenced by
the scenario's dataset names.}
}
\value{
A named list with \code{data}, \code{fit}, and \code{hc} elements: each the
corresponding task table augmented with a list column of per-task results
(\code{data} samples, \code{fits} objects, and \code{hc} tibbles respectively).
}
\description{
Executes the three task tables in dependency order - \code{data}, then \code{fit}, then
\code{hc} - via \code{purrr::pmap()} loops, threading each step's output into the next
and returning the collected per-step results. This is the no-frills baseline:
it runs in-process, with \strong{no} \code{targets} dependency, \strong{no} shard grouping or
\code{partition_by}, and \strong{no} Parquet I/O. It is also \strong{not} reproducible - no
per-task RNG seeding happens here (that arrives with the \code{state-primitives}
roadmap step); pin the ambient RNG (e.g. \code{\link[withr:with_seed]{withr::with_seed()}}) for
deterministic draws.
}
\details{
Because the scenario stores only dataset \emph{names}, the actual data frames are
supplied via \code{data} (an \code{\link[=ssd_data]{ssd_data()}} collection or named list) and looked up
by name. \code{min_pmix} names are resolved against \code{ssdtools} until the registry
roadmap step lands.
}
\examples{
scenario <- ssd_define_scenario(
  ssddata::ccme_boron,
  nsim = 1L,
  nrow = 6L,
  seed = 42L,
  dists = "lnorm"
)
withr::with_seed(42L, {
  out <- ssd_run_scenario_baseline(scenario, ssddata::ccme_boron)
})
out$hc
}
