% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-estimate.R
\name{ssd_estimate_cost}
\alias{ssd_estimate_cost}
\title{Estimate a Scenario's Compute Cost and Longest Task}
\usage{
ssd_estimate_cost(scenario, calibration = ssd_cost_calibration())
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{calibration}{An \code{ssdsims_cost_calibration}; defaults to the shipped
\code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}}. Pass the result of \code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}} for an
architecture-specific estimate.}
}
\value{
An \code{ssdsims_cost_estimate} object: a list with \code{total} and \code{longest}
(both \code{difftime} time quantities), a \code{breakdown} tibble grouped by
\code{ci_method} x \code{nboot}, and the calibration's \code{provenance}.
}
\description{
Predicts, \emph{before} a scenario is launched, roughly how much compute it costs
and how long its single longest task runs. It expands the scenario into its hc
task table read-only (via \code{\link[=ssd_scenario_hc_tasks]{ssd_scenario_hc_tasks()}}, without running any fit or
bootstrap), applies the calibrated per-task cost model, and returns the
ballpark serial \strong{total} cost and the \strong{duration of the longest single task},
plus a per-axis breakdown of which \code{ci_method}/\code{nboot} cells dominate.
}
\details{
\code{proportion} and \code{est_method} are \emph{free} axes: one bootstrap per
\verb{nboot x ci_method x parametric} cell serves every \code{proportion}/\code{est_method},
so adding values along those axes does not change the estimate. The estimator
does \strong{not} execute the scenario, draw random numbers, or alter any result.

The longest task is the irreducible wall-time floor under any amount of
parallelism; wall-time under \code{n} workers is roughly
\code{max(longest_task, total / n)}, computed by the caller.
}
\examples{
scenario <- ssd_define_scenario(
  ssd_scenario_data(ssddata::ccme_boron),
  nsim = 10L,
  seed = 42L,
  ci = TRUE,
  nboot = c(1000L, 5000L, 10000L, 50000L),
  nrow = c(5L, 10L, 20L, 50L)
)
ssd_estimate_cost(scenario)
}
\seealso{
\code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}} and \code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}}.
}
