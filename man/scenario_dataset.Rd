% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{scenario_dataset}
\alias{scenario_dataset}
\title{Isolate a Materialised Dataset from a Scenario by Name}
\usage{
scenario_dataset(scenario, name)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{name}{A scalar string naming one of the scenario's datasets.}
}
\value{
The materialised dataset tibble stored under \code{name}.
}
\description{
Returns the validated, materialised dataset tibble stored on \code{scenario} under
\code{name}. The dataset was validated (a numeric \code{Conc} column) and materialised
at construction by \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}, so this accessor performs no
registration, persistence, or re-validation - it just isolates the value a
shard body fits. Aborts with an informative error when \code{name} is not one of
the scenario's datasets.
}
\details{
Names - not values - drive task hashing (\code{TARGETS-DESIGN.md} section 1.1):
the task path carries the dataset \emph{name} and this accessor resolves it back to
the tibble at run time, so the tibble never enters a task identity.
}
\examples{
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 1L, seed = 42L)
scenario_dataset(scenario, "ccme_boron")
}
\seealso{
\code{\link[=scenario_min_pmix]{scenario_min_pmix()}} for the \code{min_pmix} counterpart.
}
