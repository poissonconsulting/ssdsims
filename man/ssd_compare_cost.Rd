% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-analysis.R
\name{ssd_compare_cost}
\alias{ssd_compare_cost}
\title{Compare Predicted Against Observed Compute Cost}
\usage{
ssd_compare_cost(
  scenario,
  root = scenario_results_dir(scenario),
  store = NULL,
  calibration = ssd_cost_calibration()
)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{root}{The run's results root (the \code{fit}/\code{hc} shard trees live under it).
Defaults to \code{\link[=scenario_results_dir]{scenario_results_dir()}} for \code{scenario}.}

\item{store}{Optional path to the run's \code{targets} data store, to add the
per-shard envelope overhead. \code{NULL} (the default) reads the shards only.}

\item{calibration}{An \code{ssdsims_cost_calibration} for the prediction; defaults
to the shipped \code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}}.}
}
\value{
An \code{ssdsims_cost_comparison}.
}
\description{
Places the \code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}} prediction beside the \code{\link[=ssd_analyse_cost]{ssd_analyse_cost()}}
observation for one scenario+run and reports the predicted and observed total
compute, the predicted and observed longest task, and the predicted/observed
ratio for each. Read-only.
}
\seealso{
\code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}}, \code{\link[=ssd_analyse_cost]{ssd_analyse_cost()}}.
}
