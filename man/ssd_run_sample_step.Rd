% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets-runner.R
\name{ssd_run_sample_step}
\alias{ssd_run_sample_step}
\title{Run a sample Shard}
\usage{
ssd_run_sample_step(tasks, scenario, out_dir)
}
\arguments{
\item{tasks}{A tibble of the shard's task rows (the \code{tasks} list-column of a
row of \code{\link[=ssd_scenario_sample_shards]{ssd_scenario_sample_shards()}}), each carrying its axis values,
\code{sample_id}, \code{seed}, and \code{primer}.}

\item{scenario}{The \code{ssdsims_scenario} (a referenced global in \verb{_targets.R}).}

\item{out_dir}{The \code{sample} results root (e.g. \code{"results/sample"}).}
}
\value{
The shard's Parquet path (the \code{format = "file"} contract).
}
\description{
Runs the \code{sample} tasks bundled into one shard: under one
\code{\link[=local_dqrng_backend]{local_dqrng_backend()}} scope, reads each task's dataset off the scenario via
\code{\link[=scenario_dataset]{scenario_dataset()}}, draws \code{n_max} rows with the per-task \verb{(seed, primer)}
through \code{sample_data_task_primer()}, and writes one Parquet at the shard's
Hive partition path. Each task's draw is tagged with its \code{sample_id} and a
\code{.row} order index so a downstream \code{fit} shard can isolate and re-order it.
}
\examples{
library(dqrng)
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 1L, seed = 42L)
shards <- ssd_scenario_sample_shards(scenario)
dir <- tempfile()
ssd_run_sample_step(shards$tasks[[1L]], scenario, file.path(dir, "sample"))
}
\seealso{
\code{\link[=ssd_run_fit_step]{ssd_run_fit_step()}}, \code{\link[=ssd_run_hc_step]{ssd_run_hc_step()}}, \code{\link[=ssd_scenario_sample_shards]{ssd_scenario_sample_shards()}}.
}
