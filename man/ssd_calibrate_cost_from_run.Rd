% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-analysis.R
\name{ssd_calibrate_cost_from_run}
\alias{ssd_calibrate_cost_from_run}
\title{Recalibrate the Cost Model from an Observed Run}
\usage{
ssd_calibrate_cost_from_run(
  scenario,
  root = scenario_results_dir(scenario),
  host = NULL
)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{root}{The run's results root (the \code{fit}/\code{hc} shard trees live under it).
Defaults to \code{\link[=scenario_results_dir]{scenario_results_dir()}} for \code{scenario}.}

\item{host}{Optional CPU description (a \code{.host} value) to select when the run
spans more than one host. \code{NULL} (the default) requires a single-host run.}
}
\value{
An \code{ssdsims_cost_calibration} with run-derived provenance.
}
\description{
Re-fits the per-task cost model from a run's \strong{measured} hc task durations
(the cost-analysis timings), returning an \code{ssdsims_cost_calibration} of the
same shape \code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}} produces - so it drops straight into
\code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}} - but derived from real measurements rather than the
synthetic micro-benchmark. The fixed addend comes from the measured fit task
durations. Read-only: no pipeline, no RNG, no writes.
}
\details{
Because the calibration is architecture-specific, timings from different
\code{.host} values are never silently pooled: a run spanning more than one host
requires an explicit \code{host}, or the function aborts listing the hosts found.
}
\seealso{
\code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}}, \code{\link[=ssd_analyse_cost]{ssd_analyse_cost()}}.
}
