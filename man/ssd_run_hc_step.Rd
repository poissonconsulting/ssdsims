% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets-runner.R
\name{ssd_run_hc_step}
\alias{ssd_run_hc_step}
\title{Run an hc Shard}
\usage{
ssd_run_hc_step(tasks, scenario, fit_dir, out_dir)
}
\arguments{
\item{tasks}{A tibble of the shard's \code{hc} task rows (from
\code{\link[=ssd_scenario_hc_shards]{ssd_scenario_hc_shards()}}), each carrying its hc-grid values, \code{hc_id}, the
parent \code{fit} path-axis values and \code{fit_id}, \code{seed}, and \code{primer}.}

\item{scenario}{The \code{ssdsims_scenario} (a referenced global in \verb{_targets.R}).}

\item{fit_dir}{The \code{fit} results root the parent shards were written to.}

\item{out_dir}{The \code{hc} results root (e.g. \code{"results/hc"}).}
}
\value{
The shard's Parquet path.
}
\description{
Runs the \code{hc} tasks bundled into one shard: for each task, opens its parent
\code{fit} shard's Parquet by partition path, isolates that task's fit by \code{fit_id},
deserialises the \code{fitdists} object, and estimates the hazard concentration
with the per-task \verb{(seed, primer)} through \code{hc_data_task_primer()}. Each
task's hc tibble (one or more rows - the \code{proportion} fan-out and the
\code{ci = FALSE} collapse, section 1.2) is tagged with its \code{hc_id} and parent
\code{fit_id}, stacked, and written as one Parquet at the shard's partition path.
}
