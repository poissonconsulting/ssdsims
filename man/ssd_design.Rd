% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{ssd_design}
\alias{ssd_design}
\title{Assemble and Validate a Design of Scenarios}
\usage{
ssd_design(...)
}
\arguments{
\item{...}{One or more \code{ssdsims_scenario} objects from
\code{\link[=ssd_define_scenario]{ssd_define_scenario()}}, optionally named.}
}
\value{
An \code{ssdsims_design} object: a named list of \code{ssdsims_scenario} objects.
}
\description{
Collects one or more \code{\link[=ssd_define_scenario]{ssd_define_scenario()}} scenarios into a validated, named
collection - the \strong{design} (design-of-experiments sense: the set of
conditions to run), the union of regular per-scenario grids into one
possibly-non-regular design. The design is turned into a single \code{targets}
pipeline by \code{\link[=ssd_design_targets]{ssd_design_targets()}}.
}
\details{
Names are taken from the argument names where supplied, otherwise derived from
the argument expression by symbol capture (e.g. a variable \code{base} becomes
\code{"base"}), mirroring \code{\link[=ssd_scenario_data]{ssd_scenario_data()}}. Each name is a \strong{scenario name}
within the design, used only as the \code{scenario} identity column in the combined
summary and the per-scenario summary target-name suffix - never in a shard
path, a shard target name, the per-task primer, or any result value. Names must
be unique, non-empty, and safe to serve as a target-name suffix (start with a
letter; letters, digits, and underscore only). A design of \strong{one} scenario is
valid and uniformly shaped - the recommended starting point for a study that
may grow (see the \emph{migration} vignette).
}
\section{Consistency contract}{

Because \code{\link[=ssd_design_targets]{ssd_design_targets()}} addresses shards by cell and shares a cell
across members (computing it once), the same \strong{name} must denote the same
value across members, or two members could disagree on a shared cell's bytes.
\code{ssd_design()} therefore validates across members that the same \code{dataset} name
binds identical data, the same \code{min_pmix} name binds an identical function, the
same \code{distset} name binds identical members, and that \code{partition_by} is
identical - aborting with an informative error otherwise. The \code{seed} may vary
across members (it becomes a \verb{seed=} results level); members sharing a \code{seed}
share their coincident cells (common random numbers).
}

\examples{
data <- ssd_scenario_data(ssddata::ccme_boron)
coarse <- ssd_define_scenario(data, nsim = 2L, seed = 42L, nrow = c(5L, 10L))
dense <- ssd_define_scenario(data, nsim = 2L, seed = 42L, nrow = c(6L, 7L, 8L))
ssd_design(coarse, dense)
}
\seealso{
\code{\link[=ssd_define_scenario]{ssd_define_scenario()}}, \code{\link[=ssd_design_targets]{ssd_design_targets()}}.
}
