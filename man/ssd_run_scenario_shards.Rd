% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shard-runner.R
\name{ssd_run_scenario_shards}
\alias{ssd_run_scenario_shards}
\title{Run a Scenario over Hive-partitioned Parquet Shards (single core)}
\usage{
ssd_run_scenario_shards(scenario, dir = tempfile("ssdsims-shards-"))
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{dir}{A results root to write the Hive-partitioned shards under; created
if absent. Defaults to a per-run session temp dir (the shards are left on
disk for inspection and reuse). The runner \strong{owns} the \code{sample}/\code{fit}/\code{hc}
subtrees under \code{dir} and clears them on each run, so replaying a scenario
with a changed \code{partition_by}/\code{bundle} never leaves stale-granularity shards
beside the new ones. (The \code{targets} pipeline instead isolates each layout
under its own \code{\link[=scenario_results_dir]{scenario_results_dir()}} root.)}
}
\value{
An \code{ssdsims_shard_run} object: a list with \code{dir} and the written
\code{sample}, \code{fit}, and \code{hc} shard Parquet paths (one per shard).
}
\description{
Executes a scenario's three task steps in dependency order - \code{sample}, then
\code{fit}, then \code{hc} - materialising each step's results as \strong{one Parquet per
\code{partition_by} path cell} under a Hive-partitioned tree
\verb{<dir>/<step>/<axis=value>/.../part.parquet}, and linking steps by reading the
parent step's shards back via \code{duckplyr} (predicate pushdown), rather than
threading results in memory. This is the single-core, \code{targets}-free sibling
of \code{\link[=ssd_run_scenario_baseline]{ssd_run_scenario_baseline()}} and the first consumer of \code{partition-by}'s
path/inner split (\code{\link[=scenario_dataset]{scenario_dataset()}}'s sibling \code{scenario_partition_axes()}).
}
\details{
It reuses the per-task seed-and-run wrappers, so for a fixed \code{scenario$seed}
it is reproducible and \strong{order-independent}, and its per-task results are
\strong{byte-identical} to \code{\link[=ssd_run_scenario_baseline]{ssd_run_scenario_baseline()}} - \code{partition_by} is a free
re-layout that moves only file paths, never results. The \strong{m:n} parent-shard
dependency (a child shard reading several parent shards, or a parent shard
feeding several children, per the section 5 coarsening defaults) is resolved
at read time: each task opens the parent shard at its \verb{<parent>_id} identity
projected onto the parent's path axes and filters to the rows it needs.

No \code{targets}, \code{crew}, manifest, or cloud upload - this is the plain-R storage
loop only, de-risking \code{hive-partitioning}/\code{task-tables}.
}
\examples{
data <- ssd_scenario_data(ssddata::ccme_boron)
scenario <- ssd_define_scenario(
  data,
  nsim = 1L,
  nrow = 6L,
  seed = 42L,
  dists = "lnorm"
)
run <- ssd_run_scenario_shards(scenario)
run$hc
}
\seealso{
\code{\link[=ssd_run_scenario_baseline]{ssd_run_scenario_baseline()}} (the in-memory reference oracle),
\code{\link[=ssd_scenario_sample_shards]{ssd_scenario_sample_shards()}}, \code{\link[=ssd_run_sample_step]{ssd_run_sample_step()}}.
}
