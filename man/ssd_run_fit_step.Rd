% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets-runner.R
\name{ssd_run_fit_step}
\alias{ssd_run_fit_step}
\title{Run a fit Shard}
\usage{
ssd_run_fit_step(tasks, scenario, sample_dir, out_dir)
}
\arguments{
\item{tasks}{A tibble of the shard's \code{fit} task rows (from
\code{\link[=ssd_scenario_fit_shards]{ssd_scenario_fit_shards()}}), each carrying its fit-grid values, \code{fit_id},
the parent \code{sample} path-axis values, \code{seed}, and \code{primer}.}

\item{scenario}{The \code{ssdsims_scenario} (a referenced global in \verb{_targets.R}).}

\item{sample_dir}{The \code{sample} results root the parent shards were written to.}

\item{out_dir}{The \code{fit} results root (e.g. \code{"results/fit"}).}
}
\value{
The shard's Parquet path.
}
\description{
Runs the \code{fit} tasks bundled into one shard: for each task, opens its parent
\code{sample} shard's Parquet by partition path, isolates that task's draw by
\code{sample_id} (restoring row order), truncates it inline (\code{head(sample, nrow)},
RNG-free, section 5), and fits with the per-task \verb{(seed, primer)} through
\code{fit_data_task_primer()} (resolving \code{min_pmix} off the scenario via
\code{\link[=scenario_min_pmix]{scenario_min_pmix()}}). The fitted \code{fitdists} object is serialised into a
\code{fit_blob} string column keyed by \code{fit_id}, and one Parquet is written at the
shard's partition path.
}
