% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-primer.R
\name{task_primer}
\alias{task_primer}
\title{Derive a Per-task Primer from its Parameters}
\usage{
task_primer(params)
}
\arguments{
\item{params}{A plain named list of task parameters, or a single-row data
frame (one task-table row).}
}
\value{
An integer vector of length 2 -- the per-task primer -- to pass as
the \code{stream} argument of \code{\link[dqrng:dqset.seed]{dqrng::dqset.seed()}} (via \code{\link[=local_dqrng_state]{local_dqrng_state()}}).
}
\description{
Derives the per-task \strong{primer} -- a length-2 integer vector -- from
\code{rlang::hash(params)}, suitable for the \code{stream} argument of
\code{\link[dqrng:dqset.seed]{dqrng::dqset.seed()}}. Together with the scenario \code{seed}, the primer fully
specifies a task's RNG starting point:
\code{dqrng::dqset.seed(seed, stream = task_primer(params))}. It pairs with
\code{\link[=local_dqrng_state]{local_dqrng_state()}}, which installs the \verb{(seed, primer)} pair under an
active \code{\link[=local_dqrng_backend]{local_dqrng_backend()}} scope.
}
\details{
The primer packs 64 bits of the \code{rlang::hash()} digest (xxhash128) as
\code{c(hi32, lo32)}. Each 32-bit half is encoded as a signed int32, with the
reserved bit pattern \code{0x80000000} (INT_MIN, which R cannot represent as a
non-\code{NA} integer) mapped to \code{NA_integer_}; dqrng accepts \code{NA_integer_} in
\code{stream} and treats it as INT_MIN, so the encoding recovers the full 64 bits
of stream entropy.

\code{params} may be a plain named list or a single-row data frame (one row of a
\verb{\{sample,fit,hc\}_tasks} table). A data-frame row is normalised to a
canonical plain list -- the inverse of \code{tibble::tibble_row()} -- by dropping
all attributes, unwrapping length-1 list-style columns to their element, and
leaving df-style (nested data-frame) columns as data frames, before hashing.
The primer is therefore identical whether derived from the row or from the
equivalent plain list. Note that \code{rlang::hash()} is order-sensitive, so the
plain list must use the \strong{same name order} as the task-table columns to
reproduce the row's primer (assembling \code{params} in a canonical column order
is part of the \code{task-tables} caller contract below).

\code{task_primer()} normalises \strong{structure, not meaning}: it hashes whatever
\code{params} it is given. The canonical, name-keyed representation is a caller
contract assembled where \code{params} is built (\code{task-tables}, over the
\code{task-lists} tables). Per the three-step model the RNG-consuming steps each
take a primer over their task identity:
\itemize{
\item \strong{sample} -- keyed \verb{(dataset, sim, replace)} only. \code{nrow} is deliberately
absent: every \code{nrow} shares one draw (sized by the scenario's \code{nrow_max}
setting) that the \code{fit} step truncates inline (\code{head(sample, nrow)},
RNG-free, no separate primer), so excluding \code{nrow} is load-bearing for the
sub-truncation property (\code{TARGETS-DESIGN.md} §5).
\item \strong{fit} -- the parent \code{sample} identity plus \code{nrow} and the fit-grid row
(\code{rescale}, \code{computable}, \code{at_boundary_ok}, \code{min_pmix} name,
\code{range_shape1}, \code{range_shape2}). \code{nrow} IS part of the fit primer: a fit on
a different truncation is a genuinely different computation.
\item \strong{hc} -- the parent \code{fit} identity plus the hc-grid row (\code{nboot},
\code{est_method}, \code{ci_method}, \code{parametric}). \code{ci} is a scalar hc setting
applied uniformly and read from the scenario, not part of the task
identity, so it is never a primer field (nor a task-row column).
}

Function-valued parameters (e.g. \code{min_pmix}) MUST be referenced \strong{by name},
not by function value, so a recompile or JIT does not move a task's primer.

The primer is derived from \code{\link[rlang:hash]{rlang::hash()}}, whose digest is \strong{not stable
across \code{rlang} versions} (a fast within-session identity hash, not a
portable checksum). Primers -- and therefore simulation seeds -- can change
when \code{rlang} is updated. Reproducibility is guaranteed by pinning the
execution environment (including the \code{rlang} version) for a given simulation
run, not by the digest being portable across versions.
}
\examples{
task_primer(list(dataset = "boron", sim = 1L, replace = FALSE))
}
\seealso{
\code{\link[=local_dqrng_state]{local_dqrng_state()}}, \code{\link[=local_dqrng_backend]{local_dqrng_backend()}}.
}
