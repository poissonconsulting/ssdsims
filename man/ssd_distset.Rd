% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distset.R
\name{ssd_distset}
\alias{ssd_distset}
\title{Assemble One or More Distribution Sets}
\usage{
ssd_distset(...)
}
\arguments{
\item{...}{One or more named character vectors, each a distribution set (its
name labels the pool, its values are the distribution names averaged
together).}
}
\value{
An \code{ssdsims_distset} object: a validated, named list of
distribution-name character vectors.
}
\description{
Constructs a validated, named \code{ssdsims_distset} collection - the single entry
point \code{ssd_define_scenario(dists = ...)} accepts. A distribution \strong{set} is the
pool of distributions model-averaged together to form one SSD (one \code{est}); a
\strong{collection} is a named list of such sets, each supplied as a \code{...} argument
whose name labels the set.
}
\details{
The fit step fits the \strong{union} of every set's members once (the single
model-averaged superset every pool is a subset of); the hc step then subsets
that one union fit down to each set's members and re-averages, so several pools
share one fit rather than re-fitting (\code{distset} is an hc-level axis - see
\code{\link[=ssd_scenario_hc_tasks]{ssd_scenario_hc_tasks()}}). The set \strong{name} is what hashes onto the hc task
path (mirroring the by-name treatment of \code{min_pmix} and datasets); the member
vectors are carried for execution only and never enter a task hash.

Set names are taken from the \code{...} argument names and must be unique,
non-missing, and filesystem-safe (each becomes a \verb{distset=<name>} Hive path
segment). Each set must be a non-empty, unique, non-\code{NA} character vector whose
members are a subset of \code{\link[ssdtools:ssd_dists_all]{ssdtools::ssd_dists_all()}}.
}
\examples{
ssd_distset(BCANZ = ssdtools::ssd_dists_bcanz())
ssd_distset(
  BCANZ = ssdtools::ssd_dists_bcanz(),
  Iwasaki = c("burrIII3", "gamma", "llogis", "lnorm", "weibull"),
  lnorm = "lnorm"
)
}
\seealso{
\code{\link[=ssd_define_scenario]{ssd_define_scenario()}}, \code{\link[=scenario_distset]{scenario_distset()}}.
}
