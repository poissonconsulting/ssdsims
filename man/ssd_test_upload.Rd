% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{ssd_test_upload}
\alias{ssd_test_upload}
\title{Probe an Upload Destination's Credentials and Connectivity}
\usage{
ssd_test_upload(upload)
}
\arguments{
\item{upload}{An upload destination from \code{\link[=ssd_upload_azure]{ssd_upload_azure()}} or
\code{\link[=ssd_upload_dryrun]{ssd_upload_dryrun()}}.}
}
\value{
\code{NULL}, invisibly (called for its side effect: the probe).
}
\description{
The front-door check, dispatched on the upload object's class, that confirms
\strong{before any compute} whether the destination is reachable and the
credentials are in the right place (\code{TARGETS-DESIGN.md} section 6.1). Run it
as a one-liner at the prompt before \code{tar_make()}, or let
\code{\link[=ssd_scenario_targets]{ssd_scenario_targets()}} run it once up front for you.
}
\details{
For an Azure destination it resolves the credentials from the environment
and, when a required variable is \strong{absent}, aborts with a loud error
\strong{naming the missing variable} (rather than failing later on a worker); when
they are present it lists the container and writes then deletes a small
marker blob, returning invisibly on success and aborting with the backend's
diagnostic on failure. For a dry-run destination it succeeds trivially
without resolving credentials or reaching any network.
}
\examples{
ssd_test_upload(ssd_upload_dryrun())
}
\seealso{
\code{\link[=ssd_upload_shard]{ssd_upload_shard()}}, \code{\link[=ssd_open_uploaded]{ssd_open_uploaded()}}, \code{\link[=ssd_upload_azure]{ssd_upload_azure()}}.
}
