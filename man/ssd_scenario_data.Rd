% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{ssd_scenario_data}
\alias{ssd_scenario_data}
\title{Assemble and Validate Datasets for a Simulation Scenario}
\usage{
ssd_scenario_data(...)
}
\arguments{
\item{...}{One or more data frames, optionally named, and/or \code{\link[=ssd_gen]{ssd_gen()}}
collections (unnamed, or spliced with \verb{!!!}). Each data frame is
validated for a numeric \code{Conc} column.}
}
\value{
An \code{ssdsims_data} object: a named list of validated tibbles.
}
\description{
Collects one or more datasets into a validated, named collection - the
single entry point through which \code{\link[=ssd_define_scenario]{ssd_define_scenario()}} takes dataset
input. Each dataset must carry a numeric \code{Conc} column (the species
sensitivity distribution convention); additional columns are preserved.
}
\details{
Names are taken from the argument names where supplied, otherwise derived
from the argument expression by symbol capture (e.g. \code{ssddata::ccme_boron}
becomes \code{"ccme_boron"}). A literal with no derivable name (e.g. a bare
\code{data.frame(...)} call) must be given an explicit name. Names must be
unique across the collection.

Generator-style inputs (a \code{fitdists} or \code{tmbfit} object, a generator
function, or a function-name string) enter the collection through
\code{\link[=ssd_gen]{ssd_gen()}}, which materialises each, once, to a reproducible \code{Conc}
tibble. Its result composes with the data-frame inputs in two equivalent
ways: passed as an unnamed argument, the collection is flattened in
(each materialised tibble becomes a member under its own name); or spliced
with \verb{!!!} (\code{\link[rlang:list2]{rlang::list2()}} splicing), with identical results:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ssd_scenario_data(boron = ccme_boron, ssd_gen(synth = ssd_rlnorm, .n = 30, .seed = 1L))
ssd_scenario_data(boron = ccme_boron, !!!ssd_gen(synth = ssd_rlnorm, .n = 30, .seed = 1L))
}\if{html}{\out{</div>}}

A materialised generator dataset is an ordinary tibble in the collection,
indistinguishable downstream from a data-frame dataset.
}
\examples{
ssd_scenario_data(ssddata::ccme_boron)
ssd_scenario_data(boron = ssddata::ccme_boron, cadmium = ssddata::ccme_cadmium)
}
\seealso{
\code{\link[=ssd_gen]{ssd_gen()}}, \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.
}
