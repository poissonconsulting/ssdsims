% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dqrng-backend.R
\name{local_dqrng_backend}
\alias{local_dqrng_backend}
\title{Local dqrng pcg64 Backend}
\usage{
local_dqrng_backend(.local_envir = parent.frame())
}
\arguments{
\item{.local_envir}{\verb{[environment]}\cr The environment to use for scoping.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Activates the dqrng \code{pcg64} RNG backend for the duration of the calling
frame, then resets it when \code{.local_envir} exits. While active, base R's
\code{runif()}, \code{rnorm()}, \code{rbinom()}, \code{rexp()}, \code{rgamma()}, \code{rpois()},
\code{sample.int()}, and \code{sample()} (and therefore \code{\link[dplyr:slice_sample]{dplyr::slice_sample()}} and
\verb{ssdtools::ssd_r*()}) draw from dqrng's \code{pcg64}, seeded via
\code{\link[dqrng:dqset.seed]{dqrng::dqset.seed()}}. \code{pcg64} is forced explicitly because it accepts the
length-2 \code{stream} argument the per-task primer design relies on; dqrng's own
default (\verb{Xoroshiro128++}) does not.
}
\details{
Registering the backend is a process-global side effect that also advances
base R's \code{.Random.seed}. \code{local_dqrng_backend()} follows the withr
convention (compare \code{\link[withr:local_seed]{withr::local_seed()}}): it pairs activation with
deferred reset so the backend is always restored, including on error.
}
\examples{

local_dqrng_backend()
dqrng::dqset.seed(42, stream = c(1L, 2L))
runif(3)
}
\seealso{
\code{\link[withr:local_seed]{withr::local_seed()}}, \code{\link[dqrng:dqset.seed]{dqrng::dqset.seed()}}.
}
