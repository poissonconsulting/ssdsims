% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{ssd_cost_calibration_default}
\alias{ssd_cost_calibration_default}
\title{Default Cost Calibration Object}
\format{
An \code{ssdsims_cost_calibration} object: a list with \code{coefficients} (a
tibble of \code{ci_method}, \code{base}, \code{slope}, \code{n0}), \code{nrow_factor} (a tibble of
\code{nrow}, \code{factor}), \code{fixed_addend} (a scalar), and \code{provenance} (cpu, R
version, ssdtools version, date, sweep grid).
}
\source{
Fitted by \code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}} during package development
(Intel Xeon @ 2.10 GHz, R 4.5.3, ssdtools 2.6.0.9002).
}
\usage{
ssd_cost_calibration_default
}
\description{
The \code{ssdsims_cost_calibration} object shipped with the package and returned by
\code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}}. It carries the per-\code{ci_method} cost-model
coefficients (\code{base}, \code{slope}, \code{n0}), the bounded \code{nrow_factor}, a
\code{fixed_addend} (sample + fit per-task overhead), and the provenance of the
machine it was fitted on. \code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}} uses it when no \code{calibration}
is supplied.
}
\details{
Because the coefficients are architecture-specific, this default yields a
\emph{ballpark} estimate sized for the machine in its provenance. Re-fit on your own
machine with \code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}} for a trustworthy estimate. It was
produced by \code{data-raw/cost_calibration.R} (which simply runs
\code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}}).
}
\seealso{
\code{\link[=ssd_cost_calibration]{ssd_cost_calibration()}}, \code{\link[=ssd_calibrate_cost]{ssd_calibrate_cost()}},
\code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}}.
}
\keyword{datasets}
