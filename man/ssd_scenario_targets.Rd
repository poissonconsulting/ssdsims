% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets-runner.R
\name{ssd_scenario_targets}
\alias{ssd_scenario_targets}
\title{Build the Targets Pipeline for a Scenario}
\usage{
ssd_scenario_targets(
  scenario,
  ...,
  root = scenario_results_dir(scenario),
  upload = NULL,
  cue = NULL
)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{...}{Unused; must be empty. Its presence forces \code{root}, \code{upload}, and
\code{cue} to be passed \strong{by name} (\code{rlang::check_dots_empty()} aborts on a
positional or misspelled argument), since \code{root} and \code{upload} are both
path-shaped and easy to transpose.}

\item{root}{The results root the shards and summary are written under;
defaults to the per-layout \code{\link[=scenario_results_dir]{scenario_results_dir()}}.}

\item{upload}{An optional upload destination (the remote-destination sibling
of \code{root}) from \code{\link[=ssd_upload_azure]{ssd_upload_azure()}} or \code{\link[=ssd_upload_dryrun]{ssd_upload_dryrun()}}, or \code{NULL}
(default) for no upload targets. See the section above.}

\item{cue}{An optional \code{targets::tar_cue()} applied to every shard target
(e.g. \code{targets::tar_cue(depend = FALSE)} to pin trusted shards against code
changes). \code{NULL} (default) uses \code{targets}' standard cue.}
}
\value{
A list of \code{targets} target objects, for \verb{_targets.R} to return.
}
\description{
A \strong{target factory}: returns the list of \code{targets} objects that runs a
scenario as a static-branching Hive-sharded pipeline (TARGETS-DESIGN.md
section 6), so a whole \verb{_targets.R} reduces to \emph{build a scenario and call
this}:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(targets)
library(tarchetypes)
library(ssdsims)
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 2L, seed = 42L)
ssd_scenario_targets(scenario)
}\if{html}{\out{</div>}}

The shard and summary targets carry \code{error = "null"} so a shard whose body
fails entirely goes \code{NULL} (its error readable via \code{tar_meta()}) without
aborting the run, and \code{ssd_summarise()} unions whatever landed
(TARGETS-DESIGN.md section 6.2). The shipped \verb{_targets.R} templates pair this
with a pipeline-wide \strong{keep-going} default (\code{tar_option_set(error = "continue")}, the \code{make -k} analogue) so an errored target skips only its
dependents while the rest of the shards still build; fail-fast pre-flight
checks (upload/cluster connectivity) belong in a separate script the user
runs \emph{before} \code{tar_make()}, not in this DAG.

For each step it \code{tarchetypes::tar_map()}s one named, \code{format = "file"},
\code{error = "null"} target per \code{partition_by} path cell (the \code{names} are the
step's path axes), and writes every shard and the summary under the
per-layout \code{\link[=scenario_results_dir]{scenario_results_dir()}} root (so a changed \code{partition_by}/\code{bundle}
never mixes shard granularities). Each step's command depends only on the
\strong{minimal scenario slice} its runner consumes (\code{scenario_step_slice()})
rather than the bare \code{scenario} global, so editing a field a step does not
read leaves the other steps' shards cached. The \code{sample} slice is built
\strong{per shard}, carrying only the dataset(s) that shard reads, so appending a
dataset mints a new shard and leaves every existing shard cached.
}
\section{Invalidation model}{

The shard targets use \strong{content-hash invalidation over their \code{format = "file"} Parquet outputs} (TARGETS-DESIGN.md section 8), observable as
\strong{cache-by-existence}: a shard is up to date iff its Parquet exists \emph{and} the
inputs its body depends on - its task rows, the step's minimal scenario slice
(\code{scenario_step_slice()}), and the parent shard target(s) it reads - are
unchanged. A missing Parquet rebuilds; a recomputed shard whose bytes are
byte-identical leaves its dependents skipped.

Instead of a coarse \code{sample -> fit -> hc} \code{tar_combine()} barrier (which marks
the \emph{whole} downstream step out of date when any one parent shard changes),
each child shard target names \strong{only the specific parent shard target(s) its
tasks read} (the Option-3 per-child upstream edges of section 6), computed at
sourcing time as \code{unique(path_key(tasks, partition_by[[parent]]))} - the same
projection the runner uses to read them. So rewriting one parent
shard re-runs only the child shards that read it. \code{summary} reads the whole
\code{hc} directory, so it names every \code{hc} shard (it re-runs when any \code{hc} shard's
bytes change, and unions the survivors of a partially-failed run).
}

\section{Pinning trusted shards (\code{cue})}{

Pass \code{cue = targets::tar_cue(depend = FALSE)} to \strong{pin} the shard targets
against upstream dependency/code changes (an edited per-task primitive, a
bumped \code{ssdtools}), so trusted shards are not rebuilt by a code edit
(TARGETS-DESIGN.md section 8.3). The carve-outs still hold: a shard rebuilds
if its \code{format = "file"} Parquet is missing, if its task-table grouping
changes (the grouping is part of the command, so path-axis and inner-axis
growth still apply under the pin), or if it previously errored. Force a
refresh of chosen shards with \code{targets::tar_invalidate()} (or by deleting
their Parquet), overriding the pin (section 8.4). The default (\code{NULL}) is
\code{targets}' standard cue.

The \code{head(sample, nrow)} truncation stays folded into the \code{fit} step (no
materialised \code{data} shard): a \code{fit} shard is keyed by \code{fit_id}, which includes
\code{nrow}, so extending \code{nrow} mints new \code{fit} shards and caches the rest, and a
widened \code{max(nrow)} changes the \code{sample} shard's \code{n_max} task row, so its
bytes change and the per-child edge propagates to exactly the \code{fit} shards
that read the wider draw - no stale short draw is produced.

To parallelise the shards, set a controller (e.g. a mirai-backed
\code{crew::crew_controller_local()}) with \code{targets::tar_option_set()} in
\verb{_targets.R} before calling this - the target set is unchanged.
}

\section{Uploading shards to cloud storage (\code{upload})}{

\code{upload} is the \strong{remote-destination sibling of \code{root}} (default \code{NULL}).
With \code{upload = NULL} the pipeline contains \strong{no} upload targets -
the clean default DAG for a non-uploader. With a non-\code{NULL} upload object the
factory pairs each step shard with an \verb{upload_<step>} target in the same
\code{tar_map} (\code{format = "file"}, \code{error = "null"}), so an unchanged shard is
never re-uploaded (content-hash skip) and a per-shard upload failure isolates
to its own branch; it also pairs the \code{summary} fan-in with a single
\code{upload_summary} target (same \code{format = "file"}, \code{error = "null"} contract)
that ships the combined summary Parquet - and, when the scenario sets
\code{samples = TRUE}, the full \code{summary-samples.parquet} alongside it - with the
same content-hash skip, so the summary re-ships only when its bytes change.
Pass \code{\link[=ssd_upload_dryrun]{ssd_upload_dryrun()}} for no-op upload targets that
reach no network (exercising the DAG shape offline / in CI) or
\code{\link[=ssd_upload_azure]{ssd_upload_azure()}} to ship to Azure. The factory performs \strong{no} network
I/O and never runs the \code{\link[=ssd_test_upload]{ssd_test_upload()}} probe: it only assembles the
target list, so sourcing \verb{_targets.R} (which \code{targets} does on every
\code{tar_make()}, \code{tar_manifest()}, \code{tar_visnetwork()}, and on each worker) stays
side-effect free. Run \code{ssd_test_upload(upload)} yourself as a one-line
preflight before \code{tar_make()} to confirm credentials and connectivity up
front; a missing credential still fails loud per-shard at upload time as a
backstop. The per-task results are byte-identical across all three \code{upload}
modes; only the presence and behaviour of the upload targets differ.
}

\examples{
\dontrun{
# _targets.R
library(targets)
library(tarchetypes)
library(ssdsims)
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 2L, seed = 42L)
ssd_scenario_targets(scenario)

# Pair each shard with a (no-op) upload target, exercised offline:
ssd_scenario_targets(scenario, upload = ssd_upload_dryrun())
}
}
\seealso{
\code{\link[=scenario_results_dir]{scenario_results_dir()}}, \code{\link[=ssd_run_scenario_shards]{ssd_run_scenario_shards()}} (the
single-core, \code{targets}-free equivalent), \code{\link[=ssd_upload_azure]{ssd_upload_azure()}}.
}
