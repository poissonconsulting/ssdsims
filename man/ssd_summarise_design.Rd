% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-targets.R
\name{ssd_summarise_design}
\alias{ssd_summarise_design}
\title{Combine Per-scenario Summaries into One Design Summary}
\usage{
ssd_summarise_design(summaries, path)
}
\arguments{
\item{summaries}{A \strong{named} character vector of per-scenario compact summary
Parquet paths; the names are the scenario names within the design.}

\item{path}{The output Parquet path for the combined design summary (the
\code{format = "file"} contract).}
}
\value{
\code{path} (the \code{format = "file"} contract).
}
\description{
The design pipeline's fan-in: unions the named per-scenario compact summary
Parquets into one combined summary, tagging each row with a \code{scenario} identity
column equal to the member's name within the design. The union is performed at
the DuckDB level (each file read lazily via \code{duckplyr}, tagged, and
\code{union_all}-ed straight back out), so no per-scenario summary is collected into
R. Per-scenario files that did not land are skipped, so the combined summary
unions the surviving members (the keep-going property of \code{\link[=ssd_summarise]{ssd_summarise()}}).
}
\seealso{
\code{\link[=ssd_design_targets]{ssd_design_targets()}}, \code{\link[=ssd_summarise]{ssd_summarise()}}.
}
