% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmix.R
\name{ssd_pmix}
\alias{ssd_pmix}
\title{Assemble and Validate \code{min_pmix} Functions for a Simulation Scenario}
\usage{
ssd_pmix(...)
}
\arguments{
\item{...}{One or more single-argument functions, optionally named.}
}
\value{
An \code{ssdsims_pmix} object: a named list of validated single-argument
functions.
}
\description{
Collects one or more \code{min_pmix} functions into a validated, named collection -
the single entry point through which \code{\link[=ssd_define_scenario]{ssd_define_scenario()}} takes \code{min_pmix}
input. Each entry must be a single-argument \strong{function} (it inputs the number
of rows of data and returns a proportion between 0 and 0.5); a name-string is
\strong{not} accepted, so the constructor performs no string-to-function resolution.
}
\details{
Names are taken from the argument names where supplied, otherwise derived from
the argument expression by symbol capture (e.g. \code{ssdtools::ssd_min_pmix}
becomes \code{"ssd_min_pmix"}), mirroring \code{\link[=ssd_scenario_data]{ssd_scenario_data()}}. Names must be
unique across the collection. The name - not the function value - is what the
task path hashes; the functions ride on the scenario for execution, isolated
by name via \code{\link[=scenario_min_pmix]{scenario_min_pmix()}}.
}
\examples{
ssd_pmix(ssdtools::ssd_min_pmix)
ssd_pmix(ssd_min_pmix = ssdtools::ssd_min_pmix, strict = function(n) 0.1)
}
\seealso{
\code{\link[=ssd_define_scenario]{ssd_define_scenario()}}, \code{\link[=scenario_min_pmix]{scenario_min_pmix()}}.
}
