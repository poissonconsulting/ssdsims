% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glossary.R
\name{ssdsims-glossary}
\alias{ssdsims-glossary}
\alias{glossary}
\title{ssdsims Glossary}
\description{
Terminology used throughout \code{ssdsims}.
}
\section{RNG terms}{

\describe{
\item{seed}{A scalar integer passed to \code{\link[base:set.seed]{base::set.seed()}} to
initialize the RNG (e.g. \code{42}). Exposed via the \code{seed} argument of
\code{\link[=ssd_sim_data]{ssd_sim_data()}}, \code{\link[=ssd_fit_dists_sims]{ssd_fit_dists_sims()}}, \code{\link[=ssd_hc_sims]{ssd_hc_sims()}} and
\code{\link[=ssd_run_scenario]{ssd_run_scenario()}}, and consumed by \code{\link[=with_lecuyer_cmrg_seed]{with_lecuyer_cmrg_seed()}}
and \code{\link[=local_lecuyer_cmrg_seed]{local_lecuyer_cmrg_seed()}}.}
\item{state}{A \code{.Random.seed}-style integer vector representing the
full internal state of the RNG. For L'Ecuyer-CMRG this is a length-7
integer vector (see \code{\link[parallel:nextRNGStream]{parallel::nextRNGStream()}}). States are produced
by \code{\link[parallel:nextRNGStream]{parallel::nextRNGStream()}} /
\code{\link[parallel:nextRNGSubStream]{parallel::nextRNGSubStream()}} and by
\code{get_lecuyer_cmrg_seed_stream()}; they are consumed by
\code{\link[=with_lecuyer_cmrg_state]{with_lecuyer_cmrg_state()}} and \code{\link[=local_lecuyer_cmrg_state]{local_lecuyer_cmrg_state()}}.
A state cannot be passed to \code{\link[base:set.seed]{base::set.seed()}} because
\code{set.seed()} only consumes a single integer.}
\item{stream}{An independent sequence of pseudo-random numbers within
the L'Ecuyer-CMRG RNG, advanced via
\code{\link[parallel:nextRNGStream]{parallel::nextRNGStream()}}. Streams are designed to be
statistically independent across distinct values of the \code{stream}
argument.}
\item{sub-stream}{A subdivision of a stream, advanced via
\code{\link[parallel:nextRNGSubStream]{parallel::nextRNGSubStream()}}. Each simulation (\code{sim}) within a
given \code{stream} uses a distinct sub-stream.}
}
}

\section{Simulation terms}{

\describe{
\item{\code{sim}}{The index of a simulation replicate within a \code{stream}.}
\item{\code{nsim}}{The number of simulation replicates to perform.}
\item{\code{start_sim}}{The starting \code{sim} index; sub-streams are advanced
to this position before data is generated.}
\item{\code{stream} (argument)}{The index of the L'Ecuyer-CMRG stream to
use; distinct \code{stream} values give statistically independent
sequences.}
\item{\code{nrow}}{The number of rows (species) in each simulated dataset.}
\item{\code{replace}}{Whether the resampling that generates simulated data
is performed with replacement.}
}
}

\section{SSD terms}{

\describe{
\item{SSD}{Species sensitivity distribution: a distribution of
species-level toxicity endpoints used in ecological risk assessment.}
\item{\code{dists}}{The parametric distributions fit to the SSD data
(e.g. \code{lnorm}, \code{gamma}, \code{llogis}); see
\code{\link[ssdtools:ssd_fit_dists]{ssdtools::ssd_fit_dists()}}.}
\item{\code{fits}}{A \code{fitdists} object holding one or more fitted
distributions.}
\item{\code{hc}}{Hazard concentration: a quantile of the SSD at a given
\code{proportion} (e.g. \code{hc5} is the 5\\% quantile); see
\code{\link[ssdtools:ssd_hc]{ssdtools::ssd_hc()}}.}
\item{\code{proportion}}{The proportion of species affected at which the
hazard concentration is computed.}
\item{\code{ci}}{Whether to compute confidence intervals on hazard
concentrations.}
\item{\code{ci_method}}{The method used to compute confidence intervals
(e.g. \code{multi_fixed}, \code{weighted_samples}).}
\item{\code{nboot}}{The number of bootstrap replicates used when computing
confidence intervals.}
}
}

