% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-analysis.R
\name{ssd_analyse_cost}
\alias{ssd_analyse_cost}
\title{Analyse a Run's Observed Compute Cost}
\usage{
ssd_analyse_cost(scenario, root = scenario_results_dir(scenario), store = NULL)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{root}{The run's results root (the \code{fit}/\code{hc} shard trees live under it).
Defaults to \code{\link[=scenario_results_dir]{scenario_results_dir()}} for \code{scenario}.}

\item{store}{Optional path to the run's \code{targets} data store, to add the
per-shard envelope overhead. \code{NULL} (the default) reads the shards only.}
}
\value{
An \code{ssdsims_cost_analysis}: a list with \code{total} and \code{longest} (both
\code{difftime}), a \code{breakdown} tibble grouped by \code{ci_method} x \code{nboot},
\code{fit_seconds} (the measured fit addend), the \code{hosts} seen, a \code{measured} flag,
the per-shard \code{envelope} (when a \code{store} is given), and provenance.
}
\description{
Reads the per-task \code{.start}/\code{.end}/\code{.host} timings a completed run left in its
\code{fit}/\code{hc} shard Parquets (the cost-analysis instrumentation), and attributes
the \strong{observed} compute to the scenario's \code{ci_method} x \code{nboot} axes - the
measured counterpart to \code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}}'s prediction. It is strictly
read-only: it reads result Parquets (and, optionally, a \code{targets} meta store),
and never runs the pipeline, fits a distribution, draws random numbers, or
writes a file. The observed \strong{total} is serial-equivalent compute (the sum of
per-task durations), distinct from elapsed wall time under parallel workers.
}
\details{
Given a \code{targets} \code{store}, it additionally reads each shard target's wall
\code{seconds} from \code{targets::tar_meta()} and reports the per-shard \strong{envelope
overhead} (\verb{target seconds - sum(task durations)}: parent read, Parquet write,
and dispatch), the number that informs \code{partition_by} tuning. The combined
\code{summary} and \verb{upload_<step>} targets are excluded; errored/unbuilt
(\code{NA}-seconds) targets are dropped from totals; unmatched stored targets are
reported, never silently dropped.
}
\seealso{
\code{\link[=ssd_estimate_cost]{ssd_estimate_cost()}}, \code{\link[=ssd_compare_cost]{ssd_compare_cost()}},
\code{\link[=ssd_calibrate_cost_from_run]{ssd_calibrate_cost_from_run()}}.
}
