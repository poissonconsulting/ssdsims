% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{ssd_upload_azure}
\alias{ssd_upload_azure}
\alias{ssd_upload_dryrun}
\title{Upload Destinations for a Scenario's Shards}
\usage{
ssd_upload_azure(url, container)

ssd_upload_dryrun()
}
\arguments{
\item{url}{The Azure Blob Storage account endpoint, e.g.
\code{"https://<account>.blob.core.windows.net"} (a non-empty string).}

\item{container}{The blob container name (a non-empty string).}
}
\value{
An S3 object of class \code{c("ssdsims_upload_azure_blob", "ssdsims_upload")} (for \code{ssd_upload_azure()}) or
\code{c("ssdsims_upload_dryrun", "ssdsims_upload")} (for \code{ssd_upload_dryrun()}).
}
\description{
Typed, self-validating destination objects for the targets pipeline's
per-shard upload (\code{TARGETS-DESIGN.md} section 6.1). Pass one to
\code{\link[=ssd_scenario_targets]{ssd_scenario_targets()}}'s \code{upload} argument (the remote-destination sibling
of \code{root}) to pair each step shard with an \verb{upload_<step>} target.
}
\details{
\code{ssd_upload_azure()} describes an Azure Blob Storage container;
\code{ssd_upload_dryrun()} is a no-op destination that reaches no network, so the
upload DAG shape can be exercised offline and in CI without credentials. Both
return a plain, serialisable S3 object of class
\code{c("ssdsims_upload_<backend>", "ssdsims_upload")} that carries \strong{only the
destination} - never credentials, open connections, or environments - so it
travels unchanged to \code{crew} workers and through \code{targets}.

Credentials stay \strong{external} to the object: the Azure methods
(\code{\link[=ssd_test_upload]{ssd_test_upload()}}, \code{\link[=ssd_upload_shard]{ssd_upload_shard()}}, \code{\link[=ssd_open_uploaded]{ssd_open_uploaded()}}) resolve
them from the environment at call time (\code{AZURE_STORAGE_ACCOUNT} plus one of
\code{AZURE_STORAGE_KEY}, \code{AZURE_STORAGE_SAS}, or the service-principal trio
\code{AZURE_TENANT_ID}/\code{AZURE_CLIENT_ID}/\code{AZURE_CLIENT_SECRET}), and abort with a
loud error naming the missing variable when a required one is absent.
}
\examples{
ssd_upload_azure("https://acct.blob.core.windows.net", "ssdsims-results")
ssd_upload_dryrun()
}
\seealso{
\code{\link[=ssd_test_upload]{ssd_test_upload()}}, \code{\link[=ssd_upload_shard]{ssd_upload_shard()}}, \code{\link[=ssd_open_uploaded]{ssd_open_uploaded()}},
\code{\link[=ssd_scenario_targets]{ssd_scenario_targets()}}.
}
