% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-lists.R
\name{ssd_scenario_fit_tasks}
\alias{ssd_scenario_fit_tasks}
\title{Derive the fit Task Table from a Scenario}
\usage{
ssd_scenario_fit_tasks(scenario)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}
}
\value{
An \code{ssdsims_tasks} object recording the \code{"fit"} step, with one row per
\verb{(dataset, sim, replace, nrow)} identity crossed with the fit grid.
}
\description{
Crosses each sample-task identity (\code{dataset}, \code{sim}, \code{replace}) with the
scenario's \code{nrow} values and each row of the scenario's \code{fit} argument grid
(\code{rescale}, \code{computable}, \code{at_boundary_ok}, \code{min_pmix} name, \code{range_shape1},
\code{range_shape2}). \code{nrow} is a genuine \code{fit} cross-join axis: the \code{fit} step
truncates its parent sample inline (\code{head(sample, nrow)}, RNG-free) before
fitting, so the shared draw is sub-truncated without a separate \code{data} step
(\code{TARGETS-DESIGN.md} section 5). Parent-identity columns are preserved
verbatim so the table can be grouped directly downstream. \code{min_pmix} is
referenced by name, not by function value (\code{TARGETS-DESIGN.md} section 1.1).
}
\details{
Each row carries a \code{fit_id} primary key and a \code{sample_id} foreign key
referencing its parent sample task.
}
\examples{
scenario <- ssd_define_scenario(
  ssddata::ccme_boron,
  nsim = 3L,
  seed = 42L,
  rescale = c(FALSE, TRUE)
)
ssd_scenario_fit_tasks(scenario)
}
