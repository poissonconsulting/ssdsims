% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{ssd_write_manifest}
\alias{ssd_write_manifest}
\title{Write a Per-Scenario Manifest}
\usage{
ssd_write_manifest(scenario, dir)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}

\item{dir}{The results directory to write \code{manifest.json} into (created if it
does not exist).}
}
\value{
The path to the written \code{manifest.json}, invisibly.
}
\description{
Writes \verb{<dir>/manifest.json}, a small JSON sidecar recording the scenario's
declarative fields - \code{seed}, \code{datasets}, \code{min_pmix}, \code{fit}, \code{hc}, and
\code{partition_by} - together with the \strong{complete session info} captured at
write time (R version, platform, and every attached/loaded package version,
via \code{\link[sessioninfo:session_info]{sessioninfo::session_info()}}), serialised as a \code{session_info} block (a
\code{platform} list plus a \code{name -> version} package map). The three
bit-stability-critical versions (\code{r_version}, \code{dqrng_version},
\code{ssdtools_version}) are also surfaced as a flat convenience subset for the
reproducibility contract (\code{TARGETS-DESIGN.md} section 8.5/section 9). This is
the manifest's stable \emph{head}; \code{\link[=ssd_assemble_manifest]{ssd_assemble_manifest()}} adds the accreting
\code{completed_shards} tail after a run.
}
\details{
The session info is \strong{descriptive}: the manifest records the toolchain a
result set was produced under, so a re-run's drift is diagnosable rather than
guessable. It does not \emph{enforce} a version on read - a mismatch is a signal
for the replay/verify layer, not an abort here. Falls back to
\code{\link[utils:sessionInfo]{utils::sessionInfo()}} when \code{sessioninfo} is unavailable.
}
\seealso{
\code{\link[=ssd_read_manifest]{ssd_read_manifest()}}, \code{\link[=ssd_record_shard]{ssd_record_shard()}},
\code{\link[=ssd_assemble_manifest]{ssd_assemble_manifest()}}.
}
\keyword{internal}
