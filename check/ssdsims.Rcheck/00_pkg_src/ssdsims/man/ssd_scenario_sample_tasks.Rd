% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-lists.R
\name{ssd_scenario_sample_tasks}
\alias{ssd_scenario_sample_tasks}
\title{Derive the sample Task Table from a Scenario}
\usage{
ssd_scenario_sample_tasks(scenario)
}
\arguments{
\item{scenario}{An \code{ssdsims_scenario} from \code{\link[=ssd_define_scenario]{ssd_define_scenario()}}.}
}
\value{
An \code{ssdsims_tasks} object (a classed tibble recording the \code{"sample"}
step) with one row per \verb{(dataset, sim, replace)} cell, a \code{sample_id} key,
and a carried \code{n_max} column.
}
\description{
Expands an \code{ssdsims_scenario} into the \code{sample} task table: one row per cell
of the cross-join of the scenario's dataset names, replicate index (\code{1:nsim}),
and \code{replace} values. Each row is the single random draw of \code{n_max = max(nrow)} rows that every \code{nrow} value sub-truncates (\code{TARGETS-DESIGN.md}
section 5), so \code{nrow} is \strong{not} a sample axis - the draw is shared. \code{n_max} is
carried as an ordinary integer column. The derivation performs no
random-number generation and adds no \code{seed}/\code{primer}/\code{stream} columns (those
arrive in later roadmap steps; see \code{TARGETS-DESIGN.md} section 2).
}
\details{
Each row carries a path-style \code{sample_id} primary key.
}
\examples{
scenario <- ssd_define_scenario(ssddata::ccme_boron, nsim = 3L, seed = 42L)
ssd_scenario_sample_tasks(scenario)
}
